case "$(uname -s)" in
    Darwin)
        pbcopy
        ;;

    Linux)
        xclip -selection clipboard
        ;;

    CYGWIN* | MINGW32* | MSYS* | MINGW*)
        clip
        ;;
    *)
        # echo 'Other OS'
        ;;
esac
